# Logo note

`logo.jpeg` is preserved unchanged at the project root. Its black background is embedded in the JPEG, so the interface currently presents it inside a clean white frame. Replace it later with an approved transparent PNG or SVG export for the best result; do not destructively edit the original.
