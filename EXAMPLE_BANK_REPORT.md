# تقرير بنك الأمثلة والتصفيق

## بنك الأمثلة

تم إنشاء بنك مركزي موثق في `examples-bank.js`. مصدر النص هو Tanzil Quran Text v1.1 (Simple)، وكل مثال مرتبط برقم السورة والآية ويحمل أجزاء تلوين صريحة بدل تحليل النص بالمسافات.

| الحكم | العدد | نون ساكنة | تنوين |
|---|---:|---:|---:|
| الإظهار | 24 | 12 | 12 |
| الإدغام بغنة | 20 | 8 | 12 |
| الإدغام بغير غنة | 12 | 6 | 6 |
| الإقلاب | 18 | 9 | 9 |
| الإخفاء | 30 | 15 | 15 |
| الإجمالي الأساسي | 104 | 50 | 54 |

توجد أيضًا 4 أمثلة موثقة للإظهار المطلق داخل كلمات الإدغام الخاصة: `الدُّنْيَا`، `بُنْيَانٌ`، `صِنْوَانٌ`، و`قِنْوَانٌ`. وبإضافتها يصبح الإجمالي 108 أمثلة، موزعة بالتساوي إلى 54 نونًا ساكنة و54 تنوينًا.

## منع التكرار

- `ExampleManager` يملك `usedExampleIds` خاصًا بالجلسة الحالية.
- أمثلة الشرح ثابتة، وتُسجّل كمستخدمة عند دخول شاشة الحكم للمرة الأولى.
- الاختيار له ثلاث درجات أولوية: غير مستخدم في الجلسة، ثم غير مستخدم في النشاط الحالي، ثم أي بديل عند نفاد البنك.
- كل إعادة لتحدٍّ أو لعبة تحصل على `activityKey` جديد، فتطلب مجموعة جديدة ما دامت البدائل متاحة.
- الاختيار المتعادل يعطي أولوية للحرف الأقل ظهورًا، ثم نوع الهدف الأقل ظهورًا، ثم النوع الفرعي الأقل ظهورًا في الإدغام.
- يستخدم كل خلط خوارزمية Fisher–Yates، ولا يوجد `sort(() => Math.random() - 0.5)`.

## توزيع الإخفاء

يحتوي البنك على مثالين لكل حرف من حروف الإخفاء الخمسة عشر: مثال نون ساكنة ومثال تنوين. يختار المدير الحرف الأقل استخدامًا أولًا، ولذلك يغطي أكبر عدد من الحروف قبل العودة إلى حرف ظهر سابقًا قدر الإمكان.

## الألعاب والتحديات

أزيلت الأمثلة القرآنية الثابتة من Mini Challenges وألعاب «اكتشف الحكم»، «أين الحكم»، «اكتشف الدخيل»، «صح أم خطأ»، «اسحب المثال»، «اكشف الجواب»، وأسئلة الأمثلة في الاختبار النهائي. بقيت الأسئلة النظرية الثابتة فقط لأنها لا تحتوي مثالًا قرآنيًا.

## التصفيق

- الملف الموجود فعليًا: `صوت_تصفيق_جمهور_للمونتاج_مؤثراتMP3_70K.mp3`.
- `SfxManager` ينشئ كائن `Audio` واحدًا مخزنًا ويحمّله مسبقًا عند بدء التطبيق.
- `playSfxSegment("applause", 2000)` و`playApplause({duration:2000})` تعيدان التشغيل من البداية وتوقفانه بعد ثانيتين وتصفّران `currentTime`.
- أي مؤقت أو تصفيق سابق يُلغى قبل بدء تشغيل جديد.
- التصفيق هو صوت النجاح الأساسي في Mini Challenges والألعاب العامة والاختبار النهائي.
- اختيار حرف صحيح جزئيًا أو وضع عنصر واحد صحيحًا يشغل نقرة خفيفة فقط؛ التصفيق يعمل مرة واحدة عند اكتمال المرحلة أو المهمة.
- الخطأ يشغل `wrong` فقط، دون تصفيق أو Confetti.
- ملف `wrong.mp3` المشار إليه في المشروع غير موجود حاليًا؛ لذلك تبقى رسالة الخطأ مرئية بلا تصفيق ولا صوت نجاح إلى أن يُضاف الملف في مساره القديم.
- قبل تشغيل أي SFX يوقف المدير التلاوة الحالية، ولا يعمل شيء عند كتم الصوت.
- الانتقال التلقائي بعد الإجابة الصحيحة مضبوط على نحو 2150ms حتى تكتمل أول ثانيتين من التصفيق.

## نسبة النص

Tanzil Quran Text, Copyright © 2007–2021 Tanzil Project. License: Creative Commons Attribution 3.0. المصدر: https://tanzil.net/
